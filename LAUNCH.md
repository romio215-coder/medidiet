# MediDiet 운영 준비

현재 제품은 브라우저에 저장하는 개인 식사 노트입니다. 진단·처방·질환별 식품 안전성 판정 서비스가 아닙니다. 변경 코드는 배포 가능한 정적 웹앱이며, 아래 외부 설정 완료 여부와 상용 출시 승인은 별도입니다.

## 구현 및 검증 범위

- 날짜·끼니·실제 제공량 배수에 따른 식사 기록, 알려진 영양소 합계와 누락 건수 표시
- 영양표 직접 입력, 전체 JSON 내보내기, 개별/전체 삭제
- 기본 세션 저장, 명시적 기기 보관 선택, 저장 실패 및 손상 데이터 안내
- 선택적 건강 관심사와 사용자가 입력한 하루 상한만 표시. 임의의 임상 기준 없음
- KO/EN, 반응형 화면, 키보드 포커스, 의미 있는 입력 레이블
- MFDS 프록시 검색: 취소, 제한 시간, 오류 처리, 키의 서버 보관

## 식품 검색 활성화 전에

1. 과거 공개 코드에 들어간 MFDS 키는 폐기/재발급합니다. 새 키를 Git이나 공개 환경변수에 넣지 않습니다.
2. 식품안전나라에서 해당 키의 I2790 활용 신청과 정상 응답을 확인합니다.
3. Supabase `mfds-search` 함수의 서버 secret `MFDS_API_KEY`를 설정하고 배포합니다. 현재 배포·키 유효성은 이 변경에서 확인하지 못했습니다.
4. 이 프런트는 로그인 없는 GET 검색을 사용하므로, 운영자는 선택한 게이트웨이의 인증 정책을 맞추고 호출량 제한·할당량·오류 감시를 설정해야 합니다. 현재 함수의 CORS는 공개 읽기이며, 인증이나 전역 rate limit을 제공하지 않습니다. 무제한 엔드포인트를 상용으로 열지 않습니다.
5. GitHub repository variable `MFDS_PROXY_URL`에 검증한 HTTPS 엔드포인트를 설정합니다. 빌드 시 `NEXT_PUBLIC_MFDS_PROXY_URL`로 전달됩니다. 비어 있으면 직접 입력은 작동하고 검색 미연결 안내가 표시됩니다.
6. 실데이터로 검색 성공, 결과 없음, 잘못된 키, 할당량 초과, 지연 응답을 확인합니다. 키나 upstream URL을 로그에 남기지 않습니다.

## 영양 데이터 기준

[식약처 I2790 문서](https://www.foodsafetykorea.go.kr/api/openApiInfo.do?menu_grp=MENU_GRP31&menu_no=656&show_cnt=10&start_idx=1&svc_no=I2790)는 `SERVING_SIZE`를 총내용량, `SERVING_UNIT`을 총내용량단위, 영양 항목을 1회제공량당으로 기술합니다. 둘의 기준이 같다고 가정하지 않습니다. DB 결과는 조회용이며 기준량이 검증되지 않으면 바로 기록할 수 없습니다. 제품 영양표를 확인하여 직접 입력합니다. 제공량이 검증된 공급원으로 교체하면 그 기준을 어댑터와 회귀 테스트에 명시한 뒤 기록 연결을 활성화합니다.

빈 값, N/A, 음수, 단위가 붙은 값, `<0.1` 같은 한계값은 0으로 추정하지 않습니다. 칼륨·GI를 임의로 채우지 않습니다. 기존 미검증 50개 음식 예시는 제품에서 제거했습니다.

## 상용 운영 결정 사항

- 운영 주체·문의 창구와 실제 호스팅/검색 제공자의 보관 정책을 반영한 서비스 약관·개인정보 안내를 확정합니다. 현재 앱 내 안내는 구현된 저장 및 전송 동작 설명입니다.
- 건강 조언이나 질환별 추천을 다시 제공하려면 전문가 검토와 근거·개정 이력 관리가 필요합니다. 현재 범위에 그런 추천은 없습니다.
- 브라우저 저장은 계정 동기화·암호화 보관·서버 백업이 아닙니다. 기기 이동, 브라우저 삭제, 여러 탭 동시 편집 시 제약이 있습니다. JSON은 열람용 내보내기이며 가져오기 기능은 없습니다.
- 상용 이미지 사용권은 저장소 소유자가 확인해야 합니다. 기존 메디다이어트 이미지 자산을 재사용했습니다.
- 실제 모바일 기기와 지원 브라우저에서 입력·읽기·삭제·저장 실패·확대 표시를 점검합니다.

## 배포와 복구

GitHub Pages는 main에 push하면 `out` 정적 파일을 배포합니다. PR 검사 후 main 반영 여부를 결정합니다. 검색 함수를 배포하는 과정은 Pages 배포와 별개입니다. 문제 발생 시 Pages에 직전 검증 커밋을 다시 배포할 수 있지만 사용자 브라우저 기록은 서버에서 복구할 수 없습니다.
